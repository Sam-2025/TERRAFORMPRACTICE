AMI = "ami-0ddfba243cbee4564"
INSTANCE_TYPE = "t3.micro"
KEYPAIR = "github"
NAME_OF_INSTANCE = "Terraform_instance"
SUBNET = "subnet-04b493565332"
# here we can give the command (terraform plan -var-file='test.tfvars') we can run independently for example in test environment
# here we can give the command (terraform apply -var-file='test.tfvars')we can run independently for example in test environment
